# AI 外语学习辅助平台 — 开发说明

> **版本**: v1.0.0  
> **最后更新**: 2026-08-07  
> **技术栈**: React 18 + TypeScript + Vite / FastAPI + SQLAlchemy 2.0 + SQLite

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [技术栈详情](#3-技术栈详情)
4. [项目结构](#4-项目结构)
5. [环境搭建](#5-环境搭建)
6. [数据库设计](#6-数据库设计)
7. [后端 API 完整清单](#7-后端-api-完整清单)
8. [前端架构与路由](#8-前端架构与路由)
9. [AI 服务集成](#9-ai-服务集成)
10. [认证与安全](#10-认证与安全)
11. [Mock 模式说明](#11-mock-模式说明)
12. [已知限制与待办](#12-已知限制与待办)
13. [部署指南](#13-部署指南)
14. [常见问题排查](#14-常见问题排查)

---

## 1. 项目概述

AI 外语学习辅助平台是一个面向高校外语学习者的综合学习系统，涵盖听、说、读、写四大核心模块，并配备词汇语法、翻译、社区、教师后台等辅助功能。平台深度集成大语言模型（LLM）与 Azure 语音服务，实现 AI 自动批改、发音评估、智能讨论等功能。

### 核心模块

| 模块 | 功能概述 |
|------|---------|
| 听力练习 | AI 生成听力脚本 + TTS 音频、手动导入素材、精听训练（逐句播放、语速控制） |
| 口语练习 | 课堂评分、Presentation、讨论房间（AI 讨论者）、人机对话、复述练习 |
| 阅读练习 | 手动导入文本、AI 分析（摘要/难句/文化注释/词汇/结构/翻译） |
| 写作练习 | AI 六维度批改、OCR 手写识别、润色版输出、逐句纠错 |
| 词汇与语法 | 艾宾浩斯智能词汇本、词根词缀分析、AI 语法练习生成、错题本 |
| 翻译练习 | AI 四维度翻译评分（准确度/流畅度/风格/整体）、改进建议 |
| 社区 | 学习小组、学习排行榜、成就系统 |
| 教师后台 | 班级管理、学生统计、作业发布、课堂口语评分 |
| 学习档案 | 学习统计、能力雷达图、AI 学习路径推荐、AI 助教对话 |

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────┐
│                   浏览器 (前端)                    │
│  React 18 + TypeScript + Vite + Tailwind CSS     │
│  端口: localhost:5173 (Vite dev server)          │
└────────────────────┬────────────────────────────┘
                     │ HTTP (REST API + JWT Bearer)
                     ▼
┌─────────────────────────────────────────────────┐
│                FastAPI 后端                        │
│  端口: localhost:8000                             │
│  Swagger 文档: /docs                              │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐     │
│  │ 路由层    │ │ Schema层  │ │ 认证中间件    │     │
│  │ (10模块)  │ │ (Pydantic)│ │ (JWT+bcrypt) │     │
│  └────┬─────┘ └──────────┘ └──────────────┘     │
│       │                                           │
│  ┌────▼─────────────────────────────────────┐    │
│  │              服务层                        │    │
│  │  ┌──────────┐ ┌────────────┐ ┌────────┐  │    │
│  │  │ LLM 服务  │ │ Azure语音  │ │ OCR    │  │    │
│  │  │ (DeepSeek)│ │ (TTS/STT)  │ │(Tess.) │  │    │
│  │  └──────────┘ └────────────┘ └────────┘  │    │
│  └──────────────────────────────────────────┘    │
│                                                   │
│  ┌──────────────────────────────────────────┐    │
│  │  数据层: SQLAlchemy 2.0 + SQLite         │    │
│  │  (20+ 张表, 可切换 PostgreSQL)            │    │
│  └──────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘
```

### 请求流

```
用户操作 → React 组件 → api/client.ts (fetch + JWT) 
→ FastAPI 路由 → 认证依赖注入 (get_current_user)
→ 服务层 (LLM/Speech/OCR) → 数据库读写 → 返回 JSON
```

---

## 3. 技术栈详情

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.3.1 | UI 框架 |
| TypeScript | 5.5.3 | 类型安全 |
| Vite | 5.3.4 | 构建工具 + 开发服务器 |
| Tailwind CSS | 3.4.4 | 原子化 CSS |
| React Router | 6.26.0 | 前端路由 |
| Zustand | 4.5.0 | 轻量状态管理 (UI 状态) |
| Recharts | 2.12.0 | 数据可视化 (图表) |
| Lucide React | 0.400.0 | SVG 图标库 |
| date-fns | 3.6.0 | 日期格式化 |

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| FastAPI | 0.115.0 | Web 框架 |
| Uvicorn | 0.30.6 | ASGI 服务器 |
| SQLAlchemy | 2.0.35 | ORM |
| Pydantic | 2.9.2 | 数据验证 |
| pydantic-settings | 2.5.2 | 配置管理 |
| python-jose | 3.3.0 | JWT 编解码 |
| bcrypt | (直接使用) | 密码哈希 |
| httpx | 0.27.2 | 异步 HTTP 客户端 (调用 LLM API) |
| openai | 1.51.0 | OpenAI 兼容 SDK (调用 DeepSeek) |
| azure-cognitiveservices-speech | 1.40.0 | Azure 语音服务 |
| pytesseract | 0.3.13 | OCR 文字识别 |
| Pillow | 10.4.0 | 图像处理 |

---

## 4. 项目结构

```
ai-language-platform/
├── src/                          # 前端源码
│   ├── api/
│   │   └── client.ts             # 统一 API 客户端 (10 个模块)
│   ├── components/
│   │   └── layout/
│   │       └── Layout.tsx        # 侧边栏 + 顶栏布局
│   ├── contexts/
│   │   ├── AuthContext.tsx        # 认证上下文 (JWT 持久化)
│   │   └── ToastContext.tsx       # Toast 通知系统
│   ├── lib/
│   │   └── tts.ts                 # 浏览器 Web Speech API 封装
│   ├── pages/
│   │   ├── Dashboard.tsx          # 仪表盘
│   │   ├── auth/LoginPage.tsx     # 登录/注册
│   │   ├── listening/ListeningModule.tsx
│   │   ├── speaking/SpeakingModule.tsx   # 口语 (含讨论房间)
│   │   ├── reading/ReadingModule.tsx
│   │   ├── writing/WritingModule.tsx
│   │   ├── vocabulary/VocabularyModule.tsx
│   │   ├── translation/TranslationModule.tsx
│   │   ├── community/CommunityModule.tsx
│   │   ├── teacher/TeacherDashboard.tsx
│   │   └── profile/LearningProfile.tsx
│   ├── store/
│   │   └── useUIStore.ts          # Zustand (sidebarCollapsed)
│   ├── App.tsx                    # 路由配置 + ProtectedRoute
│   ├── main.tsx                   # 入口 (AuthProvider + ToastProvider)
│   └── index.css                  # Tailwind 基础样式
├── backend/
│   ├── app/
│   │   ├── auth/
│   │   │   └── security.py        # 密码哈希 + JWT + 依赖注入
│   │   ├── models/                # SQLAlchemy 数据模型
│   │   │   ├── user.py
│   │   │   ├── listening.py
│   │   │   ├── speaking.py
│   │   │   ├── reading.py
│   │   │   ├── writing.py
│   │   │   ├── vocabulary.py
│   │   │   ├── community.py       # 翻译 + 社区 + 统计
│   │   │   └── teacher.py
│   │   ├── routers/               # API 路由 (10 个模块)
│   │   │   ├── auth.py
│   │   │   ├── listening.py
│   │   │   ├── speaking.py
│   │   │   ├── reading.py
│   │   │   ├── writing.py
│   │   │   ├── vocabulary.py
│   │   │   ├── translation.py
│   │   │   ├── community.py
│   │   │   ├── teacher.py
│   │   │   └── profile.py
│   │   ├── schemas/               # Pydantic 请求/响应模型
│   │   ├── services/
│   │   │   ├── llm_service.py     # LLM 服务 (DeepSeek 集成)
│   │   │   ├── speech_service.py  # Azure 语音服务
│   │   │   └── ocr_service.py     # OCR 服务
│   │   ├── config.py              # 应用配置
│   │   ├── database.py            # 数据库引擎与会话
│   │   └── main.py                # FastAPI 入口
│   ├── .env                       # 环境变量 (含 API Key)
│   ├── .env.example               # 环境变量模板
│   └── requirements.txt           # Python 依赖
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

---

## 5. 环境搭建

### 5.1 前提条件

- **Node.js** >= 18 (推荐 20.x)
- **Python** >= 3.10 (推荐 3.12+)
- **DeepSeek API Key** (或其他 OpenAI 兼容 LLM)
- **Azure Speech Key** (可选，不配则语音功能使用 Mock/浏览器降级)
- **Tesseract OCR** (可选，不配则手写识别使用 Mock)

### 5.2 前端启动

```bash
cd ai-language-platform
npm install
npm run dev
# → localhost:5173
```

### 5.3 后端启动

```bash
cd ai-language-platform/backend

# 创建虚拟环境
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 填入 LLM_API_KEY 等密钥

# 启动
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
# → localhost:8000
# Swagger 文档 → localhost:8000/docs
```

### 5.4 环境变量配置

```ini
# backend/.env

# LLM (OpenAI 兼容接口)
LLM_API_KEY=sk-your-key-here
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# Azure Speech Service (可选)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastasia

# Tesseract OCR (可选)
TESSERACT_CMD=
OCR_LANGUAGE=eng

# 数据库 (生产环境切换 PostgreSQL)
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/ai_language_platform
```

### 5.5 默认测试账户

后端启动时自动创建：

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 学生 | student | 123456 |
| 教师 | teacher | 123456 |

### 5.6 数据库初始化

后端首次启动时，`main.py` 的 `lifespan` 函数会自动执行 `Base.metadata.create_all()` 创建所有表，并创建默认测试账户。无需手动建表。

> **注意**: 如果修改了数据模型字段（新增列），SQLite 不会自动 ALTER TABLE。需删除 `ai_language_platform.db` 文件后重启后端重建表，或使用 Alembic 进行迁移。

---

## 6. 数据库设计

### 6.1 表清单

共 20+ 张表，按模块分组：

| 模块 | 表名 | 说明 |
|------|------|------|
| 用户 | `users` | 用户信息 (学生/教师) |
| 听力 | `listening_materials` | 听力素材 |
| 听力 | `listening_exercises` | 听力练习记录 |
| 口语 | `speaking_records` | 口语练习记录 |
| 口语 | `discussion_rooms` | 讨论房间 |
| 口语 | `discussion_messages` | 讨论消息 |
| 阅读 | `reading_texts` | 阅读文本库 |
| 阅读 | `reading_exercises` | 阅读练习记录 |
| 写作 | `writing_submissions` | 作文提交与批改 |
| 写作 | `writing_peer_reviews` | 写作互评 |
| 词汇 | `vocabulary_words` | 智能词汇本 (艾宾浩斯) |
| 词汇 | `grammar_exercises` | 语法练习题 |
| 词汇 | `grammar_exercise_records` | 语法练习记录 |
| 词汇 | `wrong_answers` | 错题本 |
| 翻译 | `translation_exercises` | 翻译练习题 |
| 翻译 | `translation_records` | 翻译练习记录 |
| 社区 | `study_groups` | 学习小组 |
| 社区 | `study_group_members` | 小组成员 |
| 社区 | `achievements` | 成就系统 |
| 社区 | `learning_stats` | 学习统计 (按天聚合) |
| 教师 | `classes` | 班级 |
| 教师 | `class_members` | 班级成员 |
| 教师 | `assignments` | 作业 |
| 教师 | `classroom_speaking_scores` | 课堂口语评分 |

### 6.2 核心表结构

#### users

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) PK | UUID |
| username | String(50) UNIQUE | 用户名 |
| email | String(100) UNIQUE | 邮箱 |
| hashed_password | String(255) | bcrypt 哈希 |
| display_name | String(50) | 显示名 |
| role | String(20) | student / teacher |
| level | String(20) | beginner / intermediate / advanced |
| total_points | Integer | 总积分 |
| streak | Integer | 连续学习天数 |
| joined_at | DateTime | 注册时间 |
| is_active | Boolean | 是否启用 |

#### discussion_rooms

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) PK | UUID |
| topic | String(300) | 讨论主题 |
| description | Text | 房间描述 |
| host_id | String(36) FK→users | 房主 |
| host_name | String(100) | 房主名 |
| status | String(20) | waiting / active / completed |
| max_participants | Integer | 最大人数 |
| ai_count | Integer | AI 讨论者数量 |
| language | String(10) | 讨论语言 |
| difficulty | String(20) | 难度 |
| category | String(50) | 话题分类 |
| current_speaker | String(36) | 当前发言人 |
| speaker_queue | Text (JSON) | 参与者队列 |
| created_at | DateTime | 创建时间 |

#### writing_submissions

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) PK | UUID |
| user_id | String(36) FK→users | 用户 |
| title | String(200) | 标题 |
| type | String(20) | chart / argumentative / creative / imitation |
| prompt | Text | 写作题目 |
| content | Text | 作文内容 |
| word_count | Integer | 词数 |
| status | String(20) | submitted / reviewing / completed |
| scores | Text (JSON) | 六维度评分 |
| overall_score | Integer | 总分 |
| ai_feedback | Text | AI 反馈 |
| revised_version | Text | AI 润色版 |
| error_details | Text (JSON) | 逐句错误标注 |

### 6.3 ER 关系概览

```
users ──< listening_exercises >── listening_materials
users ──< speaking_records
users ──< discussion_rooms (host)
discussion_rooms ──< discussion_messages
users ──< reading_exercises >── reading_texts
users ──< writing_submissions ──< writing_peer_reviews
users ──< vocabulary_words
users ──< grammar_exercise_records >── grammar_exercises
users ──< wrong_answers
users ──< translation_records
users ──< study_group_members >── study_groups
users ──< achievements
users ──< learning_stats
users (teacher) ──< classes ──< class_members
classes ──< assignments
classes ──< classroom_speaking_scores
```

---

## 7. 后端 API 完整清单

### 7.1 认证模块 (`/api/auth`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 注册 (返回 token + user) |
| POST | `/api/auth/login` | 登录 (支持用户名或邮箱) |
| GET | `/api/auth/me` | 获取当前用户信息 |

### 7.2 听力模块 (`/api/listening`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/listening/materials` | 素材列表 |
| POST | `/api/listening/generate` | AI 生成听力素材 |
| POST | `/api/listening/import` | 手动导入素材 |
| POST | `/api/listening/upload-audio/{id}` | 上传音频文件 |
| GET | `/api/listening/{id}` | 获取素材详情 |

### 7.3 口语模块 (`/api/speaking`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/speaking/evaluate` | 口语评估 (LLM + 可选 Azure) |
| GET | `/api/speaking/records` | 练习记录列表 |
| POST | `/api/speaking/recommend-topics` | AI 推荐讨论主题 |
| POST | `/api/speaking/rooms` | 创建讨论房间 |
| GET | `/api/speaking/rooms` | 讨论广场列表 (支持 status/category/search) |
| GET | `/api/speaking/rooms/{id}` | 房间详情 |
| POST | `/api/speaking/rooms/{id}/join` | 加入房间 |
| POST | `/api/speaking/rooms/{id}/leave` | 离开房间 |
| DELETE | `/api/speaking/rooms/{id}` | 关闭房间 (仅房主) |
| GET | `/api/speaking/rooms/{id}/messages` | 获取消息记录 |
| POST | `/api/speaking/rooms/{id}/messages` | 发送消息 |
| POST | `/api/speaking/rooms/{id}/ai-reply` | AI 讨论者回复 |
| POST | `/api/speaking/rooms/{id}/invite` | 邀请用户 |
| GET | `/api/speaking/users/search?q=` | 搜索用户 |
| POST | `/api/speaking/rooms/{id}/next-speaker` | 轮流发言传递 |

### 7.4 阅读模块 (`/api/reading`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/reading/texts` | 文本列表 |
| POST | `/api/reading/import` | 导入文本 |
| POST | `/api/reading/analyze` | AI 实时分析 (不保存) |
| POST | `/api/reading/{id}/analyze` | AI 分析并保存 |
| GET | `/api/reading/{id}` | 获取文本详情 |

### 7.5 写作模块 (`/api/writing`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/writing/submit` | 提交作文并 AI 批改 |
| POST | `/api/writing/grade` | 仅评分不保存 (快速预览) |
| POST | `/api/writing/ocr` | OCR 手写识别 |
| GET | `/api/writing/submissions` | 作文列表 |
| GET | `/api/writing/submissions/{id}` | 作文详情 |

### 7.6 词汇与语法模块 (`/api/vocabulary`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/vocabulary/words` | 词汇本列表 |
| GET | `/api/vocabulary/words/review` | 今日待复习单词 |
| POST | `/api/vocabulary/words` | 添加单词 |
| POST | `/api/vocabulary/words/{id}/review` | 复习单词 (更新艾宾浩斯) |
| DELETE | `/api/vocabulary/words/{id}` | 删除单词 |
| POST | `/api/vocabulary/root-analysis` | 词根词缀分析 |
| POST | `/api/vocabulary/grammar/generate` | AI 生成语法练习 |
| POST | `/api/vocabulary/grammar/{id}/answer` | 提交语法答案 |
| GET | `/api/vocabulary/wrong-answers` | 错题本列表 |
| PUT | `/api/vocabulary/wrong-answers/{id}/reviewed` | 标记已复习 |

### 7.7 翻译模块 (`/api/translation`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/translation/grade` | AI 翻译评分 |
| GET | `/api/translation/records` | 翻译记录列表 |

### 7.8 社区模块 (`/api/community`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/community/groups` | 学习小组列表 |
| POST | `/api/community/groups` | 创建小组 |
| POST | `/api/community/groups/{id}/join` | 加入小组 |
| GET | `/api/community/leaderboard` | 学习排行榜 |
| GET | `/api/community/achievements` | 成就列表 |

### 7.9 教师后台 (`/api/teacher`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/teacher/classes` | 创建班级 |
| GET | `/api/teacher/classes` | 班级列表 |
| GET | `/api/teacher/classes/{id}/students` | 学生列表 |
| GET | `/api/teacher/classes/{id}/stats` | 班级统计 |
| POST | `/api/teacher/assignments` | 发布作业 |
| GET | `/api/teacher/assignments/{classId}` | 作业列表 |
| POST | `/api/teacher/speaking-scores` | 课堂口语评分 |

> 教师后台所有接口均通过 `require_teacher` 依赖注入进行角色校验。

### 7.10 学习档案与 AI 助教 (`/api/profile`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/profile/stats` | 学习统计 (周进度/能力雷达/薄弱点) |
| GET | `/api/profile/recent-activities` | 最近活动 |
| GET | `/api/profile/learning-path` | AI 学习路径推荐 |
| POST | `/api/profile/ai-assistant` | AI 助教对话 |

### 7.11 健康检查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 应用状态 |
| GET | `/api/health` | 健康检查 |

---

## 8. 前端架构与路由

### 8.1 路由配置

```
/login              → LoginPage (公开)
/                   → Dashboard (受保护)
/listening          → ListeningModule
/speaking           → SpeakingModule
/reading            → ReadingModule
/writing            → WritingModule
/vocabulary         → VocabularyModule
/translation        → TranslationModule
/community          → CommunityModule
/teacher            → TeacherDashboard
/profile            → LearningProfile
*                   → 重定向到 /
```

### 8.2 认证流程

```
main.tsx
  └─ BrowserRouter
       └─ AuthProvider (AuthContext)
            └─ ToastProvider (ToastContext)
                 └─ App
                      ├─ /login → LoginPage
                      └─ ProtectedRoute
                           └─ Layout (侧边栏 + 顶栏)
                                └─ 各页面路由
```

- **AuthContext**: 从 localStorage 读取 token，调用 `/api/auth/me` 验证，提供 `isAuthenticated` / `login` / `register` / `logout` 方法
- **ProtectedRoute**: 未登录重定向到 `/login`，加载中显示 Loading 动画
- **ToastContext**: 全局通知系统，提供 `toast.success()` / `toast.error()` / `toast.info()`

### 8.3 API 客户端架构

`src/api/client.ts` 统一管理所有后端调用：

- **Token 管理**: `setToken()` / `clearToken()` / `getToken()`，自动持久化到 localStorage
- **请求封装**: `request<T>()` 函数自动注入 Authorization 头，401 时自动清除 token
- **10 个 API 对象**: `authApi` / `listeningApi` / `speakingApi` / `readingApi` / `writingApi` / `vocabularyApi` / `translationApi` / `communityApi` / `teacherApi` / `profileApi`

### 8.4 UI 设计系统

| 设计要素 | 规格 |
|---------|------|
| 主色 | Indigo (`#6366f1`) — Purple (`#a855f7`) 渐变 |
| 字体 | Inter (无衬线) / JetBrains Mono (代码) |
| 圆角 | `rounded-xl` / `rounded-2xl` |
| 阴影 | `shadow-soft` / `shadow-glow` |
| 动画 | `animate-pulse` / `animate-spin` / shimmer / scaleIn |
| 组件库 | Card 悬停动效、Loading 骨架屏、ErrorState、EmptyState |

### 8.5 TTS 语音播放

`src/lib/tts.ts` 封装浏览器 Web Speech API，作为 Azure Speech 未配置时的降级方案：

```typescript
speak(text, accent)     // 播放语音
stop()                   // 停止
pause() / resume()       // 暂停/恢复
isSpeaking()             // 是否正在播放
getVoices()              // 获取可用语音列表
accentToLang(accent)     // 口音转语言代码
```

---

## 9. AI 服务集成

### 9.1 LLM 服务 (`llm_service.py`)

通过 OpenAI 兼容 SDK 调用 DeepSeek API。核心函数：

| 函数 | 用途 | 输出 |
|------|------|------|
| `call_llm(prompt)` | 通用文本生成 | str |
| `call_llm_json(prompt)` | 结构化 JSON 生成 | dict |
| `grade_writing(content, type, prompt, title)` | 写作批改 | 六维度评分 + 逐句纠错 + 润色版 |
| `evaluate_speaking(transcript, topic, type, ref)` | 口语评估 | 六维度评分 + 参考答案 |
| `analyze_reading(content, difficulty)` | 阅读分析 | 摘要/要点/结构/难句/翻译/文化注释/词汇 |
| `grade_translation(source, translation, direction)` | 翻译评分 | 四维度评分 + 参考译文 + 改进建议 |
| `generate_grammar_exercises(point, type, difficulty, count)` | 语法练习生成 | 题目列表 |
| `analyze_word_root(word)` | 词根词缀分析 | 词根拆解 + 同族词 |
| `generate_listening_script(topic, accent, speed, difficulty, duration)` | 听力脚本生成 | 脚本 + 词汇 |
| `recommend_discussion_topics(category, difficulty, count)` | 讨论主题推荐 | 主题列表 |
| `generate_ai_discussion_reply(topic, name, persona, messages, difficulty)` | AI 讨论回复 | 英文回复 |
| `recommend_learning_path(radar, weak_points, activities)` | 学习路径推荐 | 个性化建议 |
| `chat_with_ai_assistant(message, context)` | AI 助教对话 | 回复 |

### 9.2 写作批改评分维度

```
1. 内容与观点 (Content & Ideas)          — 0-25 分
2. 组织与结构 (Organization & Structure)  — 0-25 分
3. 词汇运用 (Vocabulary)                  — 0-25 分
4. 语法准确性 (Grammar)                   — 0-25 分
5. 句式多样性 (Sentence Variety)          — 0-25 分
6. 连贯与衔接 (Cohesion & Coherence)      — 0-25 分
```

### 9.3 口语评估评分维度

```
1. 流利度 (Fluency)
2. 准确性 (Accuracy)
3. 词汇多样性 (Lexical Resource)
4. 语法多样性 (Grammatical Range)
5. 发音 (Pronunciation)
6. 互动 (Interactive Communication)
```

### 9.4 翻译评分维度

```
1. 准确度 (Accuracy)
2. 流畅度 (Fluency)
3. 风格 (Style)
4. 整体 (Overall)
```

### 9.5 AI 讨论者角色

| 名称 | 角色设定 | 讨论风格 |
|------|---------|---------|
| Emma | 多角度分析师 | 全面客观地分析各方观点 |
| James | 强势辩论者 | 提出有力论据，推动辩论深度 |
| Sophia | 魔鬼代言人 | 故意提出反面意见，激发批判性思维 |
| Lucas | 调解者 | 总结各方观点，寻找共识 |

### 9.6 Azure 语音服务 (`speech_service.py`)

| 函数 | 用途 |
|------|------|
| `text_to_speech(text, accent, speed)` | TTS 文本转语音 |
| `speech_to_text(audio_path)` | STT 语音转文本 |
| `assess_pronunciation(audio_path, reference_text)` | 发音评估 |

> 未配置 `AZURE_SPEECH_KEY` 时，以上函数返回空值或 Mock 数据。前端使用浏览器 Web Speech API 降级实现 TTS 播放。

### 9.7 OCR 服务 (`ocr_service.py`)

| 函数 | 用途 |
|------|------|
| `recognize_handwriting(image_base64)` | 手写作文 OCR 识别 |

> 依赖 Tesseract OCR，未安装时返回 Mock 文本。

---

## 10. 认证与安全

### 10.1 认证流程

```
1. 注册/登录 → 后端生成 JWT (HS256, 24h 有效期)
2. 返回 { access_token, user }
3. 前端 setToken(token) → 存入 localStorage
4. 后续请求自动携带 Authorization: Bearer <token>
5. 后端 get_current_user 依赖 → 解码 JWT → 查询 User → 注入请求
6. 401 时前端 clearToken() → 重定向到 /login
```

### 10.2 密码安全

- 使用 `bcrypt.hashpw()` / `bcrypt.checkpw()` 直接调用（不通过 passlib，避免版本兼容问题）
- JWT 密钥配置在 `config.py` 的 `SECRET_KEY`

### 10.3 角色控制

```python
get_current_user    # 所有已登录用户
require_teacher     # 仅教师角色 (role == "teacher")
```

### 10.4 CORS 配置

`config.py` 中 `CORS_ORIGINS` 允许以下来源：

```
http://localhost:5173 ~ 5177
http://127.0.0.1:5173 ~ 5177
```

> 如果 Vite 使用其他端口，需在 `config.py` 中添加。

---

## 11. Mock 模式说明

平台设计了完善的 Mock 降级机制，在未配置外部服务密钥时仍可运行：

| 服务 | 条件 | 降级行为 |
|------|------|---------|
| LLM | `LLM_API_KEY` 为空 | 返回结构完整的模拟评分/分析数据 |
| Azure TTS | `AZURE_SPEECH_KEY` 为空 | TTS 返回空 URL，前端使用浏览器 Web Speech API |
| Azure STT | `AZURE_SPEECH_KEY` 为空 | STT 返回空字符串 |
| Azure 发音评估 | `AZURE_SPEECH_KEY` 为空 | 返回空字典 |
| OCR | Tesseract 未安装 | 返回 Mock 文本 |

> 当前已配置 DeepSeek API Key，LLM 功能完全可用。Azure Speech Key 未配置，语音相关功能使用浏览器降级方案。

---

## 12. 已知限制与待办

### 已知限制

1. **无真实音频录制**：前端口语模块的录音功能使用浏览器 MediaRecorder API，但未与后端 Azure STT 完整对接
2. **讨论房间无 WebSocket**：当前使用 5 秒轮询刷新消息，非实时推送
3. **托福真题/外刊不做爬取**：仅保留手动导入端口
4. **无考试模拟和多语言支持**：按需求裁剪

### 待办事项

- [ ] 配置 Azure Speech Key，实现真实 TTS/STT/发音评估
- [ ] 实现真实音频录制与播放（MediaRecorder → 后端 → Azure）
- [ ] 讨论房间升级为 WebSocket 实时通信
- [ ] 数据库迁移至 PostgreSQL + Alembic
- [ ] 添加单元测试与集成测试
- [ ] 实现文件上传的云存储（OSS/S3）

---

## 13. 部署指南

### 13.1 开发环境

前端和后端分别启动：

```bash
# 终端 1 — 后端
cd backend
venv\Scripts\activate
python -m uvicorn app.main:app --reload --port 8000

# 终端 2 — 前端
npm run dev
```

### 13.2 生产环境

**前端构建**：

```bash
npm run build
# 产物在 dist/ 目录，可用 nginx 部署
```

**后端部署**：

```bash
# 1. 切换数据库
# 编辑 .env:
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/ai_language_platform

# 2. 安装依赖
pip install -r requirements.txt

# 3. 使用 gunicorn (Linux) 或 IIS (Windows)
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000
```

**Nginx 反向代理示例**：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端
    location / {
        root /path/to/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Swagger 文档
    location /docs {
        proxy_pass http://127.0.0.1:8000;
    }
}
```

> 生产环境记得修改 `config.py` 中的 `SECRET_KEY` 和 `DEBUG=False`。

---

## 14. 常见问题排查

### Q: 前端 "Failed to fetch" 错误

**A**: 检查后端是否在运行（`localhost:8000`），以及 `src/api/client.ts` 中的 `API_BASE` 是否匹配后端端口。

### Q: 登录返回 500 错误

**A**: 可能是 `passlib` 与 `bcrypt` 版本不兼容。项目已改为直接使用 `bcrypt.hashpw/checkpw`，确保 `backend/app/auth/security.py` 未使用 passlib。

### Q: 创建讨论房间返回 500

**A**: 确保 `db.flush()` 在创建 DiscussionMessage 之前调用，以获取 `room.id`。

### Q: 数据模型修改后旧表不兼容

**A**: 删除 `backend/ai_language_platform.db` 文件，重启后端自动重建。

### Q: CORS 错误

**A**: 在 `backend/app/config.py` 的 `CORS_ORIGINS` 列表中添加前端实际使用的端口。

### Q: LLM 返回 float 但 Schema 期望 int

**A**: 在 Pydantic Schema 中添加 `field_validator` 自动转换类型（已在 `WritingGradeResult` 中实现）。

### Q: Vite 端口不是 5173

**A**: Vite 会自动递增端口（5174、5175...）。需在后端 `config.py` 的 `CORS_ORIGINS` 中添加对应端口。

---

> 本文档随项目迭代持续更新。如有疑问请查阅 Swagger 文档 (`/docs`) 或源代码注释。
