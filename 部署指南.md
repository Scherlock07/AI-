# AI 外语学习辅助平台 — 云部署指南

> **方案**：Vercel（前端，免费）+ Render（后端，免费）
> **总费用**：0 元
> **部署时间**：约 20 分钟

---

## 前置准备

1. 注册 [GitHub](https://github.com) 账号（用来托管代码）
2. 注册 [Vercel](https://vercel.com) 账号（可用 GitHub 登录）
3. 注册 [Render](https://render.com) 账号（可用 GitHub 登录）
4. 下载安装 [Git](https://git-scm.com/downloads)

---

## 第一步：上传代码到 GitHub

### 1.1 安装 Git

下载安装 Git for Windows：https://git-scm.com/download/win

安装后打开 **PowerShell**，验证：
```bash
git --version
```

### 1.2 配置 Git（只需一次）

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### 1.3 初始化并提交代码

```bash
cd ai-language-platform
git init
git add -A
git commit -m "AI外语学习辅助平台 v1.0"
```

### 1.4 创建 GitHub 仓库并推送

1. 打开 https://github.com/new
2. Repository name 填 `ai-language-platform`
3. 选择 **Private**（私有）
4. 不要勾选任何初始化选项
5. 点击 **Create repository**

然后执行（替换 `你的用户名`）：
```bash
git remote add origin https://github.com/你的用户名/ai-language-platform.git
git branch -M main
git push -u origin main
```

> 如果提示输入密码，GitHub 已不支持密码登录，需要用 Personal Access Token。
> 去 https://github.com/settings/tokens → Generate new token → 勾选 `repo` 权限 → 复制 token 作为密码。

---

## 第二步：部署后端到 Render

### 2.1 创建 Web Service

1. 打开 https://dashboard.render.com
2. 点击 **New +** → **Web Service**
3. 连接你的 GitHub 账号，选择 `ai-language-platform` 仓库

### 2.2 配置后端服务

| 配置项 | 填写内容 |
|--------|---------|
| Name | `ai-language-platform-api` |
| Runtime | Python 3 |
| Root Directory | `backend` |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT` |
| Instance Type | Free |

### 2.3 设置环境变量

在 **Environment** 标签页添加以下变量：

| Key | Value |
|-----|-------|
| `LLM_API_KEY` | `sk-e4fa4d4202f14f4d89fc59c4be5ab361` |
| `LLM_BASE_URL` | `https://api.deepseek.com/v1` |
| `LLM_MODEL` | `deepseek-chat` |
| `AZURE_SPEECH_KEY` | （留空，使用浏览器 TTS） |
| `AZURE_SPEECH_REGION` | `eastasia` |
| `CORS_ORIGINS` | `*` |

### 2.4 部署

1. 点击 **Create Web Service**
2. 等待构建完成（约 3-5 分钟）
3. 部署成功后，Render 会给你一个地址，如：
   `https://ai-language-platform-api.onrender.com`
4. 访问 `https://ai-language-platform-api.onrender.com/docs` 验证 API 文档能打开

> ⚠️ **记下这个后端地址**，下一步要用。

### 2.5 验证后端

在浏览器中访问：
```
https://你的后端地址.onrender.com/api/auth/login
```
如果返回 `{"detail":"Method Not Allowed"}` 说明后端在正常运行（GET 方法不允许，但服务是活的）。

---

## 第三步：部署前端到 Vercel

### 3.1 创建项目

1. 打开 https://vercel.com/new
2. 导入你的 `ai-language-platform` GitHub 仓库

### 3.2 配置项目

| 配置项 | 填写内容 |
|--------|---------|
| Framework Preset | Vite |
| Root Directory | `./`（默认根目录） |
| Build Command | `npm run build`（自动识别） |
| Output Directory | `dist`（自动识别） |
| Install Command | `npm install`（自动识别） |

### 3.3 设置环境变量（关键！）

在 **Environment Variables** 中添加：

| Key | Value |
|-----|-------|
| `VITE_API_BASE` | `https://你的后端地址.onrender.com` |

> 把 `你的后端地址` 替换为第二步获得的 Render 地址。
> 例如：`https://ai-language-platform-api.onrender.com`

### 3.4 部署

1. 点击 **Deploy**
2. 等待构建完成（约 1-2 分钟）
3. 部署成功后，Vercel 会给你一个地址，如：
   `https://ai-language-platform.vercel.app`

### 3.5 验证

1. 打开 Vercel 给的地址
2. 用 `student / 123456` 登录
3. 如果能进入仪表盘，说明部署成功！

---

## 第四步：更新环境变量文件

部署成功后，修改本地 `.env.production` 文件，填入真实地址：

```bash
# 文件：ai-language-platform/.env.production
VITE_API_BASE=https://ai-language-platform-api.onrender.com
```

以后每次修改代码，只需 `git push`，Vercel 和 Render 会自动重新部署。

---

## 常见问题

### Q: 首次访问很慢（30-60 秒）

Render 免费版在 15 分钟无访问后会休眠，首次访问需要冷启动。等 30-60 秒即可，之后正常使用就不会休眠了。

### Q: 前端能打开但登录失败（CORS 错误）

检查后端 Render 的环境变量 `CORS_ORIGINS` 是否设置为 `*`。

### Q: 登录成功但 AI 功能无响应

检查后端 Render 的环境变量 `LLM_API_KEY` 是否正确填写。

### Q: 数据丢失了

Render 免费版重新部署后文件系统会重置（SQLite 数据丢失）。如果需要持久化数据：
1. 升级 Render 付费版（$7/月）获得持久磁盘
2. 或切换到 PostgreSQL 数据库（在 Render 环境变量中设置 `DATABASE_URL`）

### Q: 如何更新代码

```bash
git add -A
git commit -m "更新说明"
git push
```

Vercel 和 Render 会自动检测到代码更新并重新部署。

### Q: 如何修改前端环境变量

在 Vercel 项目设置 → Settings → Environment Variables 中修改，然后点击 Redeploy。

---

## 部署架构图

```
用户浏览器
    │
    ├──→ Vercel (前端)
    │    https://ai-language-platform.vercel.app
    │    └── React 静态文件 (dist/)
    │
    └──→ Render (后端)
         https://ai-language-platform-api.onrender.com
         ├── FastAPI + Gunicorn
         ├── SQLite 数据库
         └── DeepSeek AI API (外部调用)
```

---

## 后续升级建议

| 需求 | 方案 | 费用 |
|------|------|------|
| 数据持久化 | Render PostgreSQL 或付费磁盘 | $7/月 |
| 自定义域名 | Vercel 绑定域名 | 域名费 ~50元/年 |
| 更快响应 | Render 付费版（不休眠） | $7/月 |
| 更多并发 | Render 付费版 + 多 Worker | $7+/月 |

---

> 部署过程中遇到任何问题，随时问我。
